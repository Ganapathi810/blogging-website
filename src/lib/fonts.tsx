import { Space_Mono,Oxanium,Sour_Gummy,Open_Sans } from "next/font/google"

export const spaceMono = Space_Mono({
  subsets : ['latin'],
  weight : ['400']
})

export const oxanium = Oxanium({
  subsets : ['latin'],
})

export const sourGummy = Sour_Gummy({
  subsets : ['latin'],
})

export const openSans = Open_Sans({
  subsets : ['latin'],
  weight : ['400']
})